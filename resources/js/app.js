import './bootstrap';
import { initFixedRacer } from './fixed-racer.js';

// Initialize the fixed pseudo-3D racing game when DOM is loaded
document.addEventListener('DOMContentLoaded', function() {
    initFixedRacer();
});
